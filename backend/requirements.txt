# --- Web framework ---
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<0.32

# --- Pydantic (models & email validation) ---
pydantic[email]>=2.7,<3
pydantic-core>=2.20,<3
pydantic-settings>=2.0,<3

# --- Security / auth ---
passlib[bcrypt]>=1.7,<2
python-multipart>=0.0.9,<0.0.12

# --- Config / env ---
python-dotenv>=1.0,<2

# --- Database (Postgres + ORM + migrations) ---
SQLAlchemy>=2.0,<3
alembic>=1.13,<1.14
psycopg[binary]==3.1.20

# --- HTTP client (for webhooks / external APIs) ---
httpx>=0.27,<0.28

# --- Optional performance ---
orjson>=3.10,<4

# --- Third-party SDKs / libs ---
openai
astral
pytz
python-dateutil
ics
stripe

# --- Testing & tooling (optional; comment out if not needed in prod) ---
# pytest>=8.0,<9
# httpx[cli]>=0.27,<0.28
# ruff>=0.5,<0.7
# black>=24.0,<25
# mypy>=1.10,<2
